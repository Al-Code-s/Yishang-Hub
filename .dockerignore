# Docker 构建上下文排除清单（backend 与 frontend 两个镜像共用，上下文都是仓库根目录）
#
# 为什么必须要有这个文件：
#   1) 本机实测 node_modules 216 MB、backend/.venv 158 MB、.tmp 243 MB——
#      不排除的话每次 build 都要先把这 ~620 MB 传给 Docker 守护进程；
#   2) `COPY frontend/ ./` 会把宿主机（Windows）装的 node_modules 一起复制进 Linux 构建阶段，
#      平台不匹配的原生模块会污染前端构建；
#   3) 本机开发密钥与数据快照不应进入生产镜像——镜像里的配置只由编排（compose）注入。

# ---- Git 与本地临时 ----
.git
.gitignore
.gitattributes
.tmp/
*.log

# ---- Python 依赖与缓存 ----
**/.venv/
**/venv/
**/__pycache__/
**/*.py[cod]
**/.pytest_cache/
**/.ruff_cache/
**/.mypy_cache/
**/.coverage
**/htmlcov/

# ---- 前端依赖与构建产物 ----
**/node_modules/
**/dist/
**/.vite/
**/coverage/
**/*.tsbuildinfo

# ---- 本机配置、密钥与数据 ----
.env
.env.*
backend/.env
db/
backend/media/
backend/staticfiles/

# ---- 文档与编辑器 / 系统文件 ----
docs/
AGENTS.md
PROJECT_SPEC.md
.idea/
.vscode/
.history/
**/.DS_Store
**/Thumbs.db
**/Desktop.ini
