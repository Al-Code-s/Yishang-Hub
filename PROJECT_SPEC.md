# 意尚智造集成平台 —— 项目规格（PROJECT_SPEC.md）

> 本文件是项目的**约束性规格摘要**，与《意尚智造集成平台项目任务书》一致。
> 各专项契约的细节在 `docs/` 下展开，冲突时以本文件的范围声明为准。

## 一、项目定位

面向服饰、服装制造及相关经营企业的**统一业务平台**：一个平台、一套账号权限、
统一主数据、多个内置业务模块、跨模块流程闭环。

**「集成」指**：功能集成、数据集成、流程集成、权限集成、报表集成、设备数据接入。

**关键边界**：采购、销售、生产、仓储、质量、设备、能源等功能**直接在本平台内部实现**。
本平台**不是**「企业购买其他 ERP/WMS/MES 后由本平台对接」的接口平台。

## 二、业务范围

| 模块 | 核心能力 |
| --- | --- |
| 基础 ERP | 采购、销售、MRP、基础成本与收付款登记 |
| MES | 工单、派工、报工、在制品、生产追溯 |
| WMS | 收货、出入库、移库、盘点、库存 |
| QMS | 检验、判定、不合格处理、质量知识库 |
| EAM/CMMS | 设备、备件、点检、保养、维修 |
| EMS | 采集、计量、能源统计、费用与告警 |
| CRM | 客户、服务、投诉、满意度 |
| SRM | 供应商准入、报价、评价 |
| EHS | 安全、环保、消防、职业健康、应急 |
| 厂内物流 | 配送任务、扫码交接、设备任务记录 |
| 平台管理 | 权限、审批、通知、审计、内部协同 |
| 终端安全管理 | 工业主机资产、安全状态、补丁整改 |

## 三、明确不做（有边界）

1. 不开发完整总账、税务申报、工资核算。
2. 不自研杀毒引擎。
3. 不替代 PLC 实时控制、安全联锁、SIS 或消防联动。
4. 未获得协议与现场条件前，不直接控制 AGV、吊挂设备等硬件。
5. 不宣称辅助管理记录自动满足全部法规要求。
6. 不默认开发 SaaS 多租户计费平台（首版：单企业、多工厂、多部门）。
7. 第一版不采用微服务、Kubernetes 或复杂数据平台。
8. **不将真实硬件未接入作为业务系统无法开发的理由。**

## 四、技术栈（已确定，不擅自变更）

| 层 | 方案 |
| --- | --- |
| 前端 | Vue 3 + TypeScript + Vite + Element Plus + Pinia + Vue Router + ECharts |
| 后端 | Python 3.12 + Django 5.2 LTS + Django REST Framework |
| 数据库 | MySQL 8.0 系列（部署镜像 `mysql:8.0`；InnoDB、utf8mb4、严格模式），Django ORM + migrations |
| 驱动 | `mysqlclient`（生产，锁定版本）；Windows 本地开发用 PyMySQL |
| 缓存/队列 | Redis + Celery + Celery Beat |
| API 文档 | drf-spectacular（OpenAPI） |
| Excel | openpyxl |
| 对象存储 | S3 兼容，默认私有 |
| 工程 | uv（提交锁定文件）、Ruff、类型检查 |
| 测试 | pytest + pytest-django（后端）、Vitest（前端）、Playwright（端到端） |
| 部署 | Gunicorn + Nginx + Docker Compose |

**架构形态**：模块化单体。

```text
Vue 前端 → Nginx → Django/DRF（业务模块 + 权限审批 + 库存服务 + 审计通知 + 采集接收）→ MySQL
Celery Worker（报表/导入导出/汇总/告警/Outbox 消费）  Redis（缓存与任务消息，非业务状态唯一存储）
```

## 五、开发原则（强制）

1. 优先保证业务正确，再优化界面与性能。
2. 优先实现完整业务链路，不优先堆积功能数量。
3. 所有业务数据必须真实持久化。
4. 所有关键操作必须在后端校验。
5. 所有跨模块操作必须有清晰的数据归属。
6. 正式看板必须从业务数据计算。
7. **模拟数据必须明确标识。**
8. 每阶段必须有实际测试和可运行成果。

## 六、代码组织（强制）

```text
core  identity  factory  masterdata  workflow  crm  sales  srm  procurement
planning  mes  wms  qms  eam  ems  ehs  logistics  iot  endpoint_security
analytics  integration
```

分层职责：View（请求/鉴权/调用服务）、Serializer（格式与字段校验）、
**Service（业务规则、事务、状态迁移）**、Selector（查询与范围过滤）、Model（结构与约束）、
Task（异步执行**已定义**的服务）。

禁止：View 写库存/MRP 逻辑；Serializer 隐藏跨模块副作用；signals 自动连锁创建关键单据；
跨模块直接改他人业务表；前端计算结果决定库存扣减；创建名为 `platform.py` 的顶层模块。

## 七、数据与事务要求（强制）

- 时间**统一按 UTC 存储**（`USE_TZ=True`），界面默认 `Asia/Shanghai`；跨日/跨月边界须测试。
- 数量 `Decimal(20,6)`；**禁止 float 累计金额与库存**；API 的 Decimal **以字符串输出**；
  舍入规则 `ROUND_HALF_UP` 且只在服务层落库前发生一次。
- 单据保存**交易单位 + 基本单位 + 换算率 + 换算后数量**。
- 关键服务用 `transaction.atomic()`，必要时 `select_for_update()`：
  先校验权限 → 再锁数据 → 按固定顺序加锁 → 锁内重校验 → 同事务更新单据/余额/流水 → 提交后返回成功。
- **尚不存在的余额行不能认为 `select_for_update()` 已提供锁保护**，
  必须结合唯一约束与并发安全创建方案（见 `docs/data-model.md` 第六节）。
- 死锁与锁超时可有限重试，但必须**重跑完整事务**并保证**幂等**。
- 已过账单据不得物理删除；审计与库存流水不得通过普通业务接口修改；**不滥用全局软删除**。
- 迁移：提交迁移文件；**生产启动不自动 `makemigrations`**；上线前做空库与已有数据升级测试；
  **明确 MySQL 部分 DDL 不能像业务事务一样完整回滚**，发布前备份并编写恢复步骤。

## 八、认证、权限与审计（强制）

- 第一版同域部署，Django Session 认证；Cookie `HttpOnly`，生产 `Secure`；
  **所有写操作与登录接口均校验 CSRF**；退出使服务端会话失效。
- **首次迁移即使用自定义 User 模型**；用户（登录与权限）与员工档案（组织、岗位、排班）分离，
  可一对一关联；不是所有员工都必须有登录账号。
- **四层权限**：菜单权限、操作权限、接口权限、数据范围。
  权限编码形如 `sales.order.submit`；数据范围含公司/指定工厂/指定部门/指定仓库/本人/自定义。
  权限合并规则必须写入文档并有测试；**系统安全限制始终生效**。
- 权限必须在以下位置执行：列表查询过滤、详情访问、修改/审批/删除、关联对象选择、
  导出与异步任务、附件下载、图表汇总。**不可仅凭前端传入的 `factory_id` 决定权限。**
- 安全：Argon2 密码哈希、登录限流与失败锁定、密码与设备密钥不得明文入日志、
  参数化查询、附件类型/大小/内容检查、对象存储默认私有、敏感导出留痕、
  生产 HTTPS 与 CSP 等安全响应头、职业健康等敏感信息单独授权。
- 审计记录：操作人、组织、时间、请求标识、对象类型与 ID、操作类型、关键字段变更摘要、
  原因及审批依据；**不保存完整密码、令牌**；**关键审计与业务操作同事务保存**。

## 九、接口与异步（强制）

- 统一前缀 `/api/v1/`；正确 HTTP 状态码；统一错误格式（`code`/`message`/`details`/`request_id`）；
  分页有最大页大小限制；排序与过滤字段采用白名单；OpenAPI 同步更新。
- 幂等：库存过账、扫码报工、转单、设备上报支持幂等标识；
  同一标识重复调用不产生重复结果，同标识不同内容拒绝，幂等结果与业务同事务提交。
- Outbox：事件与业务数据**同事务写入**，后台轮询分发，**至少一次**语义、消费者幂等，
  超限进入人工处理并支持有权限的重放；`transaction.on_commit()` 不能替代持久化 Outbox。
- Celery：任务可观测、有超时/重试/失败原因，**调度任务不得重复生成业务单据**，
  长任务分批处理，**数据库是任务业务结果的最终依据**。
- Excel 导入：上传 → 校验 → 错误预览 → 确认导入 → 结果报告；提供模板；每行明确错误；
  默认不静默跳过；防公式注入与恶意文件；下载链接设权限与有效期。

## 十、界面（强制）

现代企业级管理后台：深蓝/科技蓝/白/浅灰，中文界面，左侧导航、顶部工具栏、标签页、面包屑、
消息与待办、个人中心；不使用与业务无关的装饰。各角色只显示有权访问的菜单；
**尚未实施的模块不展示伪可用页面**，可在管理员进度页标记规划状态。

## 十一、分阶段实施

| 阶段 | 主要交付 | 验收重点 |
| --- | --- | --- |
| 0 | 仓库检查、需求矩阵、架构、模型、环境 | 前后端与 MySQL 可启动 |
| 1 | 登录、权限、组织、主数据、基础审批、日志 | 登录到主数据维护闭环 |
| 2 | 客户、供应商、采购、销售、WMS | 采购入库到销售出库 |
| 3 | BOM、工艺、MRP、MES、QMS | 订单到生产交付 |
| 4 | 设备、备件、保养、点检、维修 | 异常到维修关闭 |
| 5 | 采集、EMS、能源报表与告警 | 模拟采集到处理闭环 |
| 6 | CRM 深化、EHS、物流、终端安全 | 服务与整改闭环 |
| 7 | 综合报表、基础成本深化、性能、安全、运维 | 完整验收与恢复演练 |

补充：阶段 2 如需质检放行，**应先实现最低可用的待检、放行状态**；
备件主数据与库存能力由共享模块提供，**阶段 4 不再重复创建库存体系**。

## 十二、执行规则（对开发者与 AI 代理同样有效）

- 先检查仓库，**避免覆盖已有有效代码**。
- 普通不确定项记录到 `docs/assumptions.md`；涉及删除数据、生产配置、真实设备控制的事项**必须确认**。
- **不得提交生产密钥、账号密码和真实个人资料。**
- 每阶段更新进度文档（`docs/progress.md`）与需求追踪矩阵（`docs/requirements-matrix.md`）。
- **未执行的测试必须注明"未执行"，不能写成通过。**
- 不得把开发服务器当成生产部署方案。
- **不创建大量空壳模块冒充完成；不使用写死数据冒充真实报表；不伪造硬件接入结果。**

## 十三、完成标准

页面可操作、API 可访问、数据持久化、权限生效、状态迁移正确、异常处理明确、
关键操作可审计、跨模块结果正确、**自动化测试实际执行**、文档已更新、未完成内容明确列出。

**「页面已存在」不等于「已通过阶段验收」。**