# 意尚智造集成平台（Yishang Platform）

面向服饰、服装制造及相关经营企业的**统一业务平台**。

一个平台、一套账号权限、统一主数据、多个内置业务模块、跨模块流程闭环。

> **重要**：采购、销售、生产、仓储、质量、设备、能源等功能**在本平台内部实现**，
> 本平台**不是**对接外部 ERP / MES / WMS 的接口平台。

- 技术栈：Python 3.12 + Django 5.2 LTS + DRF + MySQL 8 + Redis/Celery ＋ Vue 3 + TypeScript + Element Plus
- 架构形态：**模块化单体**（第一版不使用微服务/Kubernetes）
- 完整规格：`PROJECT_SPEC.md` ｜ 开发约定：`AGENTS.md`

## 当前状态

**已完成：阶段 0（工程基座）、阶段 1（登录 → 主数据维护闭环）、阶段 2 第一步（客户与供应商主数据）、
阶段 2 核心（**统一库存服务：余额 / 流水 / 单据 / 质量放行 / 幂等过账 / 并发安全**）、
阶段 2 第三步（**采购模块：采购申请 → 订单 → 收货 → 来料检验放行**）、
阶段 2 第四步（**销售模块：销售订单 → 库存占用 → 发货出库 → 退货检验**）、
阶段 3 第一步（**BOM 与工艺路线版本快照：版本化工程数据 + 审批后冻结 + 派生新版本 + 快照输出**）、
阶段 3 第二步（**MRP：时间分段净算 + 多层 BOM 展开 + 缺料清单 + 采购建议转草稿采购申请**）、
**阶段 4（设备管理：台账 / 类型 / 零部件 / 备品备件 / 配件 / 故障报修 / 备件库存台账 / 保养 / 维修 / 点巡检 / 异常上报）**、
**阶段 5（能源管理：计量区域与仪表、水电气液价格与阈值、抄表、运行记录、报警、看板 / 报表 / 统计）**、
**阶段 6 首块（安全环保管理：安全 / 环保 / 消防 / 设备设施安全）与厂内物流（自动化设备 + 物流任务）**、
**阶段 5 设备数采首版（HTTP 上报入口、设备独立令牌与限流、双重去重、越限 / 离线告警、内置模拟器）**、
**客户管理深化（客户投诉、产品评价）**、
**采集统计与客户服务统计（按明细实时聚合的只读统计，不落汇总表）**、
**质量管理（QMS：检验项目 / 检验单与检验结果判定 / 质量报警 / 质量问题知识库 / 质量信息动态监测）**、
**生产执行（MES：生产工单下达与快照冻结 / 领料 / 报工 / 完工与完工入库）与 MRP 在制供给、生产建议转单**、
**供应商五维量化评价（权重配置与只增不改的版本派生 / 缺数据两种口径 / 评价统计）**。

| 已交付 | 说明 |
| --- | --- |
| 工程与部署 | Django + DRF 后端、Vue 3 + TS 前端、MySQL/Redis 配置、`compose.yaml` |
| 认证与会话 | 会话登录、CSRF、退出、改密、个人中心、登录限流与失败锁定 |
| 权限体系 | 四层权限（菜单/操作/接口/数据范围）、**368 个权限点**、**144 项菜单（121 个业务页面 + 23 个目录）** |
| 组织与排班主数据 | 公司、部门、工厂、车间、线体、工位、员工、班次、班组 |
| 服饰主数据 | 物料、分类、面料属性、款式、颜色、尺码、SKU、计量单位与换算、标识分型 |
| 仓储基础 | 仓库、库区、储位 |
| 统一库存服务 | 库存余额（四类数量口径）与**只追加**流水、库存单据（收货/出库/同仓移库/调整/质量）、过账与冲销、质量放行、**库存占用与释放**、幂等与并发安全（**不含跨仓调拨在途、盘点；冻结动作入口未开放**） |
| 客户与供应商主数据 | 客户档案与联系人、供应商档案与联系人、供应商资质与有效期（**不含寻源、报价与准入审批流程**） |
| 采购模块 | 采购申请（常规/计划/紧急）与审批、申请转订单、采购订单（金额后端计算、审批、关闭）、供应商准入校验与例外授权、收货（**不允许超收**、草稿占额度）、收货过账 → 待检库存、来料检验放行（**人工判定**，多行逐行放行）；**不含**询价比价、到货差异、退货、应付与付款登记、单据打印 |
| 销售模块 | 销售订单（颜色尺码 SKU、**金额后端计算**、审批、关闭/取消）、**库存占用与释放**（只减可用量、幂等、不写流水）、销售发货（**必须先占用**、不允许超发、出库过账幂等）、销售退货（先入待检 → 检验判定，**批次继承原发货维度**）、订单到交付链路查询（`/sales/orders/{id}/chain/`）；**不含**销售计划、折扣、订单变更版本快照、分销商、基础预测、应收与收款登记 |
| 计划 / 工程数据 | BOM（款式通用与 SKU 差异版本、替代料、**含损耗用量由后端计算**、生效区间、审批后冻结、**变更只能派生新版本**、版本快照）与工艺路线（工序 / 标准工时 / 设备要求 / **工序质检点**，默认工艺 裁剪→缝制→整烫→检验→包装）；**不含** 线体排产、裁剪任务与工位派工（工单与报工见「生产执行」行） |
| MRP（阶段 3 第二步） | 运行 MRP（按日 / 按周**时间分段净算**，快照落库）、**多层 BOM 展开**（低层码只净算一次、取含损耗用量）、循环 BOM 检查、可用库存（排除冻结 / 占用、只认合格）与采购在途作为供给、缺料清单与**采购 / 生产建议**、供需追溯、**采购建议转草稿采购申请**（不绕过审批）、建议取消 / 运行归档；**对库存只读**；**不含**提前期与批量规则、安全库存、替代料展开 |
| 生产执行（MES，阶段 3 第三步） | 生产工单（草稿 → 下达 → 生产中 → 已完工 → 已关闭 / 已取消）；**下达冻结 BOM / 工艺快照**、
据 BOM 展开用料（含损耗）、据工艺生成工序（含质检点）；生产领料与**完工入库经统一库存服务**；
报工（**合格 + 返工 + 报废 = 报工量**，不允许超计划，台账只读不可回改）；**质检点报满自动开 QMS 检验单**，
完工前必须判定合格 / 让步接收；编码规则 `MO` / `RPT`；**不含**线体排产、裁剪任务、工位派工、在制品转移、扫码与硬件控制 |
| 设备管理（阶段 4） | 设备类型与设备台账（含特种设备标记）、设备零部件、备品备件与配件、故障报修 → 派工 → 维修记录、保养项目 / 计划 / 任务 / 日历 / 记录（计划生成到期任务**幂等**）、点巡检项目 / 任务 / 记录（异常可**一键转报修**）、异常类型 / 任务 / 记录；备件现存量**只读自仓储库存**，备件采购复用采购申请流程；**不含**数据采集与实时监控 |
| 能源管理（阶段 5） | 计量区域（可分级）、计量设备（水 / 电 / 气 / 液）、价格（**尖峰平谷 + 生效区间**）、阈值（读数上下限 / 日用量 / 单耗 / 离线时长）；抄表（**用量按上次读数 × 倍率计算，读数回退拒绝**）、设备运行记录（运行时长 / 能耗 / 单耗）、报警（越限 / 离线 / 单耗 / 能耗，**同仪表同类型当天去重**，处理与关闭走动作接口）、能源首页 / 设备监控 / 看板 / 能耗统计 / 能耗报表（**可导出真实 xlsx 并记审计**）；**不含**硬件数据采集（当前靠人工抄表，在线 / 离线为抄表超时推断） |
| 安全环保管理（阶段 6 首块） | 安全制度、安全培训、隐患排查（**整改 → 提交验收 → 验收，不通过退回**）、应急预案、事故处理（调查 → 整改 → 关闭）；排污监测（**实测值超限自动判不达标**）、固废危废台账、环保合规检查；消防设施、消防演练、动火作业（**必须指定监护人**）；特种设备检验、检维修作业、本质安全 / 防爆防静电 / 防火防爆检查；操作日志只读 |
| 厂内物流（阶段 6） | 自动化设备（AGV / 穿梭车 / 堆垛机等）档案与状态变更、物流任务（**待下发 → 已下发 → 执行中 → 已完成 / 已取消**，故障设备不可下发、同一设备不可并行执行）、操作日志只读 |
| 客户管理深化（阶段 2/6） | 客户投诉（类型 / 级别 / 来源登记，**受理 → 处理 → 关闭只能走动作接口**，关闭必须写处理措施，可登记满意度）与产品评价（评分 1~5、回复与关闭）；两个列表页顶部有**只读统计**（总量 / 未关闭或待回复 / 平均满意度或平均评分 / 好评率 + 状态、类型、级别、来源、评分分布）；**不含**投诉自动分派与满意度问卷 |
| 供应商评价（阶段 2 深化） | 五维（质量 / 技术 / 响应 / 交付 / 成本）量化评价：权重配置（**合计必须正好 100%**，改权重**只能派生新版本**，同公司同时只有一版启用）、评价单（草稿 → 已生效 → 已归档，**五维必须各一行**，总分**只由服务层按权重快照计算**）、**缺数据两种口径**（标注缺失不重分配权重、因此**不给等级**；重新分配有效权重合计**精确 100%** 才评等级，等级线 A ≥ 90 / B ≥ 75 / C ≥ 60 / 其余 D）、**只读评价统计**（平均分**只算口径完整的评价**，缺失维度单独计数，按明细实时聚合）；**不自动回写** `Supplier.grade`（由人工决定是否同步档案） |
| 质量管理（阶段 7 首块） | 检验项目（**判定口径的唯一来源**，定量项上下限含端点）、检验单（来料 / 首件 / 过程 / 成品 / 出货，`source_no` 引用来源单据）、**检验结果由服务层按上下限自动判定**（定量项客户端传的结论被忽略，定性项必须由检验员判定）、单据状态机（草稿 → 已提交 → 已判定 → 已关闭，跳步 409）、**判定不合格自动生成质量报警**（同单只生成一条，报警未闭环时单据关不掉）、质量问题知识库（现象 / 原因 / 纠正 / 预防措施 + 来源链路，发布与归档）、**质量信息动态监测**（合格率只按已判定单据统计，让步接收单列，不合格项 TOP10）；**不含**检验标准版本快照、检测仪器直连、返工 / 报废处置工单 |
| 设备数采（阶段 5 首版） | 连接配置（协议 / 地址 / 限流 / 批次上限）→ 数采设备（网关 / 传感器 / 智能水表 / 智能电表）→ 测点 → 原始报文 → 标准化读数；**设备独立令牌**（库里只存 SHA-256 摘要，明文只显示一次、可随时轮换）、限流与批次上限、报文与读数**双重去重**、采集失败留痕、读数越限与设备离线告警（写入能源报警台账）、**模拟数据全程带「模拟」标识**、设备监控聚合页与**采集统计**（按「测点 × 时间桶」按业务时区实时聚合，按小时 / 按天，超限只标记不报警）、内置模拟器（`iot_simulate` 命令）；**只实现 HTTP 上报与模拟器**，MQTT / Modbus 与真实设备联调未做 |
| 审批与治理 | 审批模板与实例、顺序多级、条件路由、快照与轨迹；审计、通知、字典、编码规则、附件 |
| 界面 | 后台布局、按权限下发的动态菜单（**一级目录与二级页面按层级区分样式**）、工作台看板、实施进度页；统一 Element Plus 品牌主题与卡片/表格/标签页观感 |

**尚未开始**：销售计划与预测、**跨仓调拨在途与盘点**、MES 剩余的线体排产 / 裁剪任务 / 工位派工 /
在制品转移与扫码硬件控制、
**MQTT / Modbus 等工业协议解析与真实设备联调**（当前只有 HTTP 上报入口与内置模拟器）、工业终端安全、
采集原始数据的归档 / 清理任务、综合报表 / 成本 / 运维演练，以及供应商寻源与报价
—— 见 `docs/requirements-matrix.md` 的阶段标注与各阶段增量清单（设备 / 能源 / 物流 / EHS 见 §一之十八，
采集统计与客户服务统计见 §一之二十，生产执行见 §一之二十二）。

> ⚠️ **Docker Compose 尚未完整验证**：项目方已在 Windows + Docker Desktop 上实际跑过
> `docker compose build` 与 `up -d mysql`（`mysql:8.0` 起得来且 `healthy`），但**整套服务跑通、
> `migrate`、Nginx、登录流程仍未验证**。已修复 4 类实际踩到的阻塞点（构建期软件源、重复 build 目标、
> 等数据库就绪、Nginx Host 端口），详见 `docs/progress.md` §四十三～§四十七。
> 仍**不声称"已验证可部署"**；换机器部署请照抄 `docs/deployment.md` §三「新电脑从零到能用」。

## 环境要求

| 组件 | 版本 | 备注 |
| --- | --- | --- |
| Python | 3.12 | 本项目在 3.12.14 上验证 |
| Node.js | ≥ 20.19 | 本项目在 22.17.1 / npm 10.9.2 上验证 |
| MySQL | 8.0 系列（部署镜像 `mysql:8.0`） | 本地开发验证于 8.0.17，见偏差说明 |
| Redis | 7.x（目标） | 本地开发验证于 3.2.100 |

## 快速开始

### 1. 配置环境变量

仓库里**已经带好两份 `.env`**（本项目是单人私有仓库，取舍见 `docs/assumptions.md` 第 47 条），
`git clone` 下来就能直接用，**正常不需要自己创建**：

| 文件 | 谁读它 | 放什么 |
| --- | --- | --- |
| `backend/.env` | Django（`manage.py` / `runserver` / `pytest`） | 本地开发：`DB_DRIVER=pymysql`、`DB_NAME` / `DB_USER` / `DB_PASSWORD`、`DJANGO_SECRET_KEY`、初始化口令 |
| `.env`（仓库根目录） | Docker Compose | 容器部署：`MYSQL_ROOT_PASSWORD`、`HTTP_PORT` / `HTTP_BIND`、`APT_MIRROR` / `NPM_REGISTRY`、`DJANGO_*` |

两份都含本机演示库的真实口令，改动后按普通文件提交即可；文件丢失时要重建哪些键见
`docs/deployment.md` §二。**不要**用 `Copy-Item .env.example .env` 去覆盖：示例文件里必填键是空的，
覆盖后构建会直接报 `required variable ... is missing a value`。

Windows 本地开发用 `DB_DRIVER=pymysql`（无 C 编译工具链）；Docker/Linux 用 `mysqlclient`。

> 数据库请使用**专用应用账号** `yishang_app`，不要用 `root` 连接应用；
> **生产**环境的真实口令只从环境变量注入，不写进代码，也不提交到仓库。

### 2. 启动后端

```powershell
cd backend
.\.venv\Scripts\python.exe manage.py migrate
.\.venv\Scripts\python.exe manage.py bootstrap_system   # 权限点/菜单/角色/管理员
.\.venv\Scripts\python.exe manage.py seed_demo          # 兼容入口，等价于 seed_demo_xjys
.\.venv\Scripts\python.exe manage.py seed_demo_xjys     # 仅开发环境演示数据（新疆意尚智造 XJYS，2026-01 起）
.\.venv\Scripts\python.exe manage.py runserver 127.0.0.1:8000
```

初始化命令的密码来自环境变量（`YISHANG_ADMIN_PASSWORD` / `YISHANG_DEMO_PASSWORD`）；
**未设置时会生成随机密码并打印一次**，代码中不硬编码任何公开默认密码。
`seed_demo` / `seed_demo_xjys` 在 `production` 环境都会被直接拒绝。
平台只保留「新疆意尚智造科技有限公司」一家公司：`seed_demo` 已合并为 `seed_demo_xjys` 的兼容入口（两者等价），并要求先执行过 `bootstrap_system`（权限点缺失时会明确报错）。

也可使用脚本：`powershell -ExecutionPolicy Bypass -File scripts\dev_backend.ps1`

### 3. 启动前端

```powershell
cd frontend
npm install
$env:VITE_DEV_BACKEND='http://127.0.0.1:8000'
npm run dev
```

也可使用脚本：`powershell -ExecutionPolicy Bypass -File scripts\dev_frontend.ps1`

前端通过 Vite 代理把 `/api`、`/admin`、`/static`、`/media`、`/healthz`、`/readyz`
转发到 Django，保持与生产（Nginx 同域）一致的会话与 CSRF 行为。

## Docker 部署（换电脑 / 给客户装）

前提：装好并启动 **Docker Desktop**；仓库 `git clone` 下来就带着配置（`.env`）与数据快照（`db/*.sql`），
不需要再手工传任何文件。下面命令**在仓库根目录**执行：

```powershell
cd <仓库目录>
docker compose build                    # 构建 nginx + backend 两个镜像（首次 5~10 分钟）
docker compose up -d --wait mysql       # 起数据库并等健康检查通过
powershell -ExecutionPolicy Bypass -File scripts\docker_db.ps1 -Action Import -File db\yishang_platform_2026-09-24.sql
docker compose up -d                    # 起其余服务（migrate 自动执行）
docker compose exec backend python manage.py collectstatic --noinput
docker compose ps                       # mysql/redis/backend/worker/beat/nginx 都应 Up，migrate 应为 Exited (0)
```

- **访问**：本机 `http://localhost:8080/`；局域网其他电脑 `http://<本机IP>:8080/`（端口取 `.env` 的 `HTTP_PORT`）。
- **端口被占用 / 要让局域网访问**：`powershell -ExecutionPolicy Bypass -File scripts\docker_network.ps1`
  （自动挑空闲端口 + 写白名单，改完 `docker compose up -d`）。
- **容器数据备份 / 搬机器**：`powershell -ExecutionPolicy Bypass -File scripts\docker_db.ps1 -Action Export`。
- 容器里的 MySQL 与宿主开发库是**两套独立实例**（同名 `yishang_platform` 但互不影响），见 `docs/backup-restore.md` §8.6。
- 完整说明（逐条排错、数据在哪里、怎么彻底重来、为什么这么设计）见 **`docs/deployment.md` §三**。

## 访问地址

| 地址 | 说明 |
| --- | --- |
| http://127.0.0.1:5173/ | 前端界面 |
| http://127.0.0.1:5173/guide.html | **平台使用说明（网页版）**，可直接给客户看；系统内顶部与个人中心也有「使用说明」入口 |
| http://127.0.0.1:8000/api/v1/ | 后端 API |
| http://127.0.0.1:8000/api/v1/docs/ | OpenAPI（Swagger UI）；**仅供开发者使用，界面不提供入口** |
| http://127.0.0.1:8000/healthz | 存活检查 |
| http://127.0.0.1:8000/readyz | 就绪检查（含数据库） |
| http://127.0.0.1:8000/admin/ | Django Admin（运维兜底，非业务前端） |

登录账号：`bootstrap_system` 创建的管理员（默认用户名 `admin`，密码见初始化时设置或输出的随机值）。
面向客户的逐屏操作步骤与常见报错处理见 `docs/user-guide.md`（§三 登录与账号、§六 按模块操作指南、§八 常见问题与错误码）；
演示账号、启动参数与文档同步规则属开发内容，见 `docs/deployment.md` 与 `AGENTS.md` §九。

## 验证步骤

```powershell
# 一键冒烟：后端 check / 迁移检查 / ruff / pytest + 前端 typecheck / vitest / build
powershell -ExecutionPolicy Bypass -File scripts\smoke_check.ps1
```

或分别执行：

```powershell
cd backend
.\.venv\Scripts\python.exe manage.py check
.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run
.\.venv\Scripts\python.exe -m ruff check apps config tests
.\.venv\Scripts\python.exe -m pytest tests -q --reuse-db

cd ..\frontend
npm run typecheck
npm run test
npm run build
```

手工验证要点：

1. 未登录访问 `/api/v1/identity/users/` → 403。
2. 登录后能拿到会话、权限与菜单；工作台显示看板卡片。
3. 在「基础资料 → 物料档案」新建物料 → 列表可见 → 用重复编码再建 → 400 且提示明确。
4. 「系统管理 → 审计与登录日志」能看到上面的新增操作记录。
5. 退出后再次访问受保护接口 → 403。
6. 「仓储管理 → 库存余额」看到演示余额（`available = on_hand - frozen - reserved`）；
   「库存流水」**没有编辑/删除按钮**。
7. 「库存单据」新建一张 `issue` 单，数量大于可用量 → 过账被拒并提示可用量不足；
   改为可满足的数量 → 过账成功、余额与流水同步变化 → 冲销（填原因）后库存回滚、状态为 `reversed`。
8. 「采购管理 → 采购申请」新建申请 → 提交 → 审批通过 → 「转订单」→ 采购订单金额由后端算出 →
   提交并审批 → 「采购收货」从订单带出明细（数量不得超过未收量）→ 过账 →
   「库存余额」出现该批次 `quarantine` 库存 → 用质检账号执行「来料检验」判定合格 →
   余额转为 `qualified`，此后可被 `issue` 单领用。
9. 「销售管理 → 销售订单」新建订单（可含颜色尺码 SKU）→ 提交并审批 → 「库存占用」→
   「仓储管理 → 库存余额」的 `reserved` 增加、`on_hand` 不变 → 「销售发货」生成发货单并过账 →
   `on_hand` 减少、`reserved` 归零 → 「销售退货」登记并收货（进 `quarantine`）→
   用质检账号判定合格 → 库存回到 `qualified`，可被再次销售。

## 测试结果（实际执行）

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 后端测试 | `pytest backend/tests -q --reuse-db` | **494 passed**（2026-09-24 重跑，见 `docs/test-report.md` §三十八，MySQL 上运行，含文档同步、枚举标签与角色绑定用例） |
| 后端检查 | `manage.py check` | 无问题 |
| 迁移一致性 | `makemigrations --check --dry-run` | No changes detected |
| 代码风格 | `ruff check apps config tests` | All checks passed |
| 前端类型 | `npm run typecheck` | 通过（退出码 0） |
| 前端测试 | `npm run test` | **15 文件 / 255 项 passed** |
| 文档同步 | `python scripts/build_user_guide.py --check` | 网页版说明与 Markdown 源一致（`pytest` 已含该校验） |
| 前端构建 | `npm run build` | 构建成功 |

补充：`scripts/smoke_check.ps1` 7 步全过；MRP 真实 HTTP 链路 29 项检查全部通过
（对标运行中的开发服务器，见 `docs/test-report.md` §16.5）。

**未执行**（不得视为通过）：`mysqlclient` 生产驱动验证（容器内 `pytest` 未跑）、
Celery Worker/Beat 实际运行（Outbox 事件仍为 `pending`）、Playwright 端到端测试、
真实设备接入（只验证了 HTTP 上报入口与内置模拟器）、备份恢复演练、性能压测、并发转单压测，
以及 **Docker 整套服务跑通与登录流程验证**（`docker compose build` 与 `up -d mysql` 已由项目方实际执行，
`mysql:8.0` 容器已起且 `healthy`；`backend`/`nginx`/`migrate`/登录仍未验证）。
详见 `docs/test-report.md`。

## 文档地图

| 文档 | 内容 |
| --- | --- |
| `docs/user-guide.md` | **面向客户的使用说明**：登录与账号、菜单与权限、按模块操作、常见问题与错误码（只写使用者能看到的行为，不含开发内容；**发给客户先看这份**） |
| `docs/user-guide.html` | 上述说明的**单文件网页版**（离线可看、可打印/导出 PDF，可直接发给客户）；由 `python scripts/build_user_guide.py` 从 Markdown 生成 |
| `docs/requirements.md` | 范围基线与排除项 |
| `docs/requirements-matrix.md` | 逐条需求追踪矩阵 |
| `docs/architecture.md` | 架构、分层、ADR |
| `docs/data-model.md` | 数据模型与口径（含 NULL 唯一索引方案） |
| `docs/business-flows.md` | 五条业务闭环 |
| `docs/permission-matrix.md` | 权限矩阵与菜单树 |
| `docs/api-conventions.md` | 接口契约 |
| `docs/inventory-rules.md` | 库存规则（已实现，见 §八） |
| `docs/energy-calculation.md` | 能源计量（阶段 5） |
| `docs/hardware-integration.md` | 硬件接入边界 |
| `docs/assumptions.md` | 假设与偏差 |
| `docs/progress.md` | 阶段进度报告 |
| `docs/test-report.md` | 测试实际结果 |
| `docs/acceptance.md` | 阶段验收结论 |
| `docs/deployment.md` | 部署与运行 |
| `docs/backup-restore.md` | 备份与恢复 |

## 已知环境偏差

| 项 | 目标 | 实际 | 处理 |
| --- | --- | --- | --- |
| MySQL | 8.0 系列 | 8.0.17 | 项目方确认版本基线由 8.4 LTS 调整为 8.0 系列；部署镜像固定 `mysql:8.0`；代码不依赖 8.4 专属语法 |
| Redis | 7.x | 3.2.100 | 仅作缓存与 Broker，不使用新版本专属命令 |
| 数据库驱动 | mysqlclient | 开发用 PyMySQL | Docker/Linux 使用 mysqlclient（**未验证**） |
| Docker | Compose 部署 | 沙箱内守护进程不可达；项目方 Windows 机器上已部分实跑 | 文件已编写；已实跑 `build`（曾因 apt 源失败，已修）与 `up -d mysql`（`healthy`），**整套服务与登录仍未验证** |

详见 `docs/assumptions.md`。

## 下一步

1. ~~客户、供应商主数据与 SRM/CRM 基础能力~~ —— **已完成**（见 `docs/progress.md` §六）。
2. ~~采购申请 → 审批 → 采购订单 → 到货 → 待检收货 → 检验放行 → 合格入库~~
   —— **本轮已完成**（见 `docs/progress.md` §八）。剩余：询价比价、到货差异、退货、应付与付款登记、单据打印。
3. ~~统一库存服务：库存余额与流水、四类数量口径、禁止负库存、幂等过账、并发安全~~
   —— **已完成**（见 `docs/progress.md` §七）。剩余：跨仓调拨在途、盘点范围冻结、冻结/占用动作入口。
4. ~~销售订单、库存占用、发货与退货（发货接入统一库存服务，**不新建库存体系**）~~
   —— **已完成**（见 `docs/progress.md` §十）。
5. ~~BOM 与工艺路线版本快照~~ —— **已完成**（见 `docs/progress.md` §十四）。
6. ~~MRP（阶段 3 第二步）~~ —— **已完成**（见 `docs/progress.md` §十五）。
   剩余：采购提前期与批量规则、安全库存、替代料展开、
   除销售订单外的需求来源、MRP 导出与定时重算。
   —— **在制供给与生产建议转草稿工单已随 MES 完成**（见 `docs/progress.md` §三十二）。
7. ~~**MES 生产工单与报工**（阶段 3 第三步）~~ —— **已完成**（见 `docs/progress.md` §三十二）：
   下达冻结 BOM / 工艺快照、BOM 展开用料、工艺生成工序、领料与完工入库经统一库存服务、
   报工数量守恒与超产拦截、质检点自动开 QMS 检验单并作为完工硬门。
   剩余：线体排产、裁剪任务 / 裁片批次、工位派工、在制品转移与返工工单、扫码 / RFID 与硬件控制、
   工序良率与 OEE、工单成本。每轮增量的文档同步要求见 `AGENTS.md` §九。
8. **QMS 检验单**：~~检验项目 / 检验单与结果判定 / 质量报警 / 质量问题知识库~~ —— **已完成**
   （见 `docs/progress.md` §三十一）。剩余：检验标准版本快照、返工 / 报废处置工单、
   检测仪器直连；~~用工艺路线的 `is_quality_gate` 决定质检点~~ —— **已随 MES 完成**
   （见 `docs/progress.md` §三十二）；把 `procurement.receipt.inspect` 的人工判定升级为引用检验单仍未做。
9. 采购模块的后续增量：询价比价、到货差异处理、采购退货、应付与付款登记、单据打印、
   供应商准入审批流程接入 `workflow`。
10. **阶段 5 设备数采**：~~HTTP 上报入口、设备令牌与限流、报文 / 读数去重、越限与离线告警、设备监控、内置模拟器~~
    —— **已完成**（见 `docs/progress.md` §二十九）。剩余：MQTT / Modbus 协议适配（**待真实设备协议确认**）、
    原始数据归档 / 清理（**刻意不建小时 / 日汇总表**，理由见 `docs/data-model.md` §十）、工业终端安全。
11. **客户管理深化**：~~客户投诉、产品评价~~ —— **已完成**（见 `docs/progress.md` §二十九）。
12. ~~采集统计（`/api/v1/iot/statistics/`）与投诉 / 评价统计~~ —— **本轮已完成**（见 `docs/progress.md` §三十）。
    剩余：统计的定时快照与同环比、统计接口的性能压测。
13. ~~**供应商五维量化评价**（质量 / 技术 / 响应 / 交付 / 成本）~~ —— **本轮已完成**
    （见 `docs/progress.md` §三十三）；剩余：供应商寻源与报价。
14. **未做的清单项（按价值排序）**：销售计划 / 分销商 / 市场预测、跨系统数据交换中间件、跨系统数据交换中间件、工业主机资产与补丁台账、
    EMS 能源调度、EAM 的 OEE 统计、EHS 职业健康。
15. 前提：建议先在有 Docker 的环境验证 `compose.yaml`。
